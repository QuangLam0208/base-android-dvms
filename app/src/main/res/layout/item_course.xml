<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/course_card_margin_horizontal"
    android:layout_marginVertical="@dimen/course_card_margin_vertical"
    app:cardBackgroundColor="@color/course_card_background"
    app:cardCornerRadius="@dimen/course_card_corner_radius"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/course_card_padding">

        <com.google.android.material.card.MaterialCardView
            android:layout_width="match_parent"
            android:layout_height="@dimen/course_image_height"
            app:cardBackgroundColor="@color/course_card_background"
            app:cardCornerRadius="@dimen/course_image_corner_radius"
            app:cardElevation="0dp">

            <ImageView
                android:id="@+id/iv_course_avatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="Khoá học"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_course_placeholder" />
        </com.google.android.material.card.MaterialCardView>

        <TextView
            android:id="@+id/tv_course_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/course_name_margin_top"
            android:textColor="#FFFFFF"
            android:textSize="@dimen/course_name_text_size"
            android:textStyle="bold"
            tools:text="Khoá học ReactJS — Xây dựng chức năng Github với GraphQL" />

        <TextView
            android:id="@+id/tv_course_price"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/course_price_margin_top"
            android:textColor="@color/accent_color"
            android:textSize="@dimen/course_price_text_size"
            android:textStyle="bold"
            tools:text="7,500,000 đ" />

        <TextView
            android:id="@+id/tv_course_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/course_description_margin_top"
            android:ellipsize="end"
            android:lineSpacingExtra="@dimen/course_description_line_spacing"
            android:maxLines="2"
            android:textColor="@color/course_short_description"
            android:textSize="@dimen/course_description_text_size"
            tools:text="Lazy loading · code splitting · Memoization trong ReactJS. Tìm hiểu GraphQL và so sánh với REST." />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_view_more"
            android:layout_width="@dimen/course_button_width"
            android:layout_height="@dimen/course_button_height"
            android:layout_marginTop="@dimen/course_button_margin_top"
            android:backgroundTint="@color/accent_color"
            android:minHeight="0dp"
            android:paddingHorizontal="@dimen/course_button_padding_horizontal"
            android:paddingVertical="@dimen/course_button_padding_vertical"
            android:text="@string/button_more"
            android:textAllCaps="false"
            android:textColor="@color/white"
            android:textSize="@dimen/course_button_text_size"
            android:textStyle="bold"
            app:cornerRadius="@dimen/course_button_corner_radius"
            app:icon="@drawable/ic_arrow_drop_down"
            app:iconGravity="textEnd"
            app:iconPadding="@dimen/course_button_icon_padding"
            app:iconTint="@color/white" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>